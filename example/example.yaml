--- # Agador Metaervice Configuration
influxdb:
  object: influxdb.InfluxDBClient
  args:
    host: influxdb.service.consul
    port: 8086

dynamodb:
  object: boto3.resource
  args:
    service_name: dynamodb
    endpoint_url: http://dynamodb:8000/
    region_name: us-east-1
